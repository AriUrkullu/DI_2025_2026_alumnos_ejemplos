/* Declara las rutas de la aplicación (mapa de paths → componentes / lazy loading). Angular Router lo usa para decidir qué vista mostrar según la URL. */
import { Routes } from '@angular/router';
/* Routes es un tipo (interface) que exporta Angular Router, y se usa para tipar l variable routes como “un array de definiciones de rutas”. */

export const routes: Routes = [];
